/*Este programa muestra el signo zodiacal de una persona. 
Para ello el usuario debe introducir únicamente 
el día y el mes de nacimiento y el programa 
determinará inmediatamente el signo zodiacal de la persona.*/ 
#include<stdio.h>
#include<stdlb.h>
main()
{
      int mes, dia, x;
      printf("Introduce tu mes de nacimiento(del 1 a l 12)");
      scanf("%d",mes);
      printf("Escribe el tu dia de nacimiento");
      scanf("%d",dia);
      if((1 <= mes && mes <= 12) && (1 <= dia && dia <= 31))
      {
                if(mes==1 && )
                
                
      }
}
